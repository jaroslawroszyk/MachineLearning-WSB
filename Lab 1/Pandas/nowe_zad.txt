Wczytaj do ramki danych zbiór  https://archive.ics.uci.edu/ml/machine-learning-databases/iris/iris.data  (zbiór danych Iris). Użyj poniższego kodu, aby wczytać dane:

```python
import pandas as pd

columns = ['SepalLength', 'SepalWidth', 'PetalLength', 'PetalWidth', 'Species']
df = pd.read_csv('iris.data', header=None, names=columns)
```

Bezpośrednio na tej ramce danych, bez tworzenia danych pośrednich, wykonaj następujące polecenia:

1.  Usuń kolumnę 'PetalWidth' .
2.  Zmień nazwę kolumny 'SepalLength' na 'Length' .
3.  Dodaj nową kolumnę 'Area' , która będzie wynikiem mnożenia 'SepalLength' i 'SepalWidth'.
4.  Wypisz unikalne wartości w kolumnie 'Species' .
5.  Zmień wszystkie wartości w kolumnie 'PetalLength' na wartość w jednostkach centymetrów (pomnóż przez 10) .
6.  Posortuj ramkę danych według kolumn 'Species' (rosnąco) i 'SepalWidth' (malejąco) .
7.  Oblicz średnią wartość 'PetalLength' dla każdego gatunku 'Species' .
8.  Znajdź wiersze, w których 'SepalLength' jest większe niż 6, a 'Species' to 'Iris-setosa' .
9.  Zapisz zmodyfikowaną ramkę danych do nowego pliku CSV o nazwie 'modified_iris.csv' .
10.  Użyj metody `drop_duplicates()` i usuń zduplikowane wiersze w ramce danych .
11.  Zaktualizuj kolumnę 'Area' tak, aby jej wartości były zaokrąglone do dwóch miejsc po przecinku .
12.  Stwórz nową kolumnę 'IsLarge', która będzie miała wartość True, jeśli 'SepalLength' jest większe niż 5, a False w przeciwnym przypadku .
13.  Zmień indeks ramki danych na kolumnę 'Species' i wypisz nowy indeks .
14.  Znajdź i wypisz wiersze, w których wartości w kolumnach 'SepalLength' i 'SepalWidth' są większe niż średnia dla tych kolumn .
15.  Sprawdź, czy w ramce danych znajdują się wartości NaN i wypisz liczbę brakujących danych w każdej kolumnie .
16.  Przekształć ramkę danych, aby wszystkie kolumny miały wartości w skali od 0 do 1 (normalizacja) .

